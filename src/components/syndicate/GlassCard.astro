---
interface Props {
  glow?: 'red' | 'gold';
  class?: string;
  [key: string]: unknown;
}

const { glow, class: className = '', ...rest } = Astro.props;
---

<div class:list={['syndicate-glass p-6 md:p-8', glow === 'gold' && 'syndicate-glass--gold', className]} {...rest}>
  <slot />
</div>
